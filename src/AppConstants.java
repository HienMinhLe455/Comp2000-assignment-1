import java.awt.*;

/// Constants such as window size, fonts, colors, should be placed here to maintain readability.
public interface AppConstants {
    int SCREEN_WIDTH = 800;
    int SCREEN_HEIGHT = 600;
    Font DEFAULT_FONT = new Font("Arial", Font.BOLD, 16);
}
